diff --git a/ios/IntendedWidget/IntendedBasicWidget.swift b/ios/IntendedWidget/IntendedBasicWidget.swift
index 29d1c84..3085291 100644
--- a/ios/IntendedWidget/IntendedBasicWidget.swift
+++ b/ios/IntendedWidget/IntendedBasicWidget.swift
@@ -242,8 +242,10 @@ struct IntendedBasicWidget: Widget {
                 BasicWidgetEntryView(entry: entry)
             }
         }
-        .configurationDisplayName("Intended")
-        .description("Track your daily habits at a glance.")
+        // Gallery copy lives in <locale>.lproj/Localizable.strings; WidgetKit
+        // resolves the keys in the extension's own bundle.
+        .configurationDisplayName(LocalizedStringKey("widget.basic.name"))
+        .description(LocalizedStringKey("widget.basic.description"))
         .supportedFamilies([.systemSmall, .systemMedium])
     }
 }
diff --git a/ios/IntendedWidget/IntendedLockScreenWidget.swift b/ios/IntendedWidget/IntendedLockScreenWidget.swift
index d9ecc11..f55a28a 100644
--- a/ios/IntendedWidget/IntendedLockScreenWidget.swift
+++ b/ios/IntendedWidget/IntendedLockScreenWidget.swift
@@ -158,8 +158,10 @@ struct IntendedLockScreenWidget: Widget {
         StaticConfiguration(kind: kind, provider: LockScreenProvider()) { entry in
             LockScreenEntryView(entry: entry)
         }
-        .configurationDisplayName("Intended")
-        .description("Quick glance at your daily progress.")
+        // Gallery copy lives in <locale>.lproj/Localizable.strings; WidgetKit
+        // resolves the keys in the extension's own bundle.
+        .configurationDisplayName(LocalizedStringKey("widget.lockScreen.name"))
+        .description(LocalizedStringKey("widget.lockScreen.description"))
         .supportedFamilies([.accessoryCircular, .accessoryInline, .accessoryRectangular])
     }
 }
diff --git a/ios/IntendedWidget/IntendedPremiumWidget.swift b/ios/IntendedWidget/IntendedPremiumWidget.swift
index 24df271..8af4b69 100644
--- a/ios/IntendedWidget/IntendedPremiumWidget.swift
+++ b/ios/IntendedWidget/IntendedPremiumWidget.swift
@@ -475,6 +475,19 @@ struct PremiumLargeView: View {
 struct IntendedPremiumWidget: Widget {
     let kind: String = "IntendedPremiumWidget"
 
+    /// Gallery copy lives in <locale>.lproj/Localizable.strings; WidgetKit
+    /// resolves the keys in the extension's own bundle.
+    ///
+    /// Tap-to-record is `HabitRow`'s `Button(intent:)`, which exists only on
+    /// iOS 17 — below that the rows are display-only, so the gallery must not
+    /// promise a tap it can't keep.
+    private var descriptionKey: LocalizedStringKey {
+        if #available(iOS 17.0, *) {
+            return LocalizedStringKey("widget.detailed.description")
+        }
+        return LocalizedStringKey("widget.detailed.description.displayOnly")
+    }
+
     var body: some WidgetConfiguration {
         StaticConfiguration(kind: kind, provider: PremiumProvider()) { entry in
             if #available(iOS 17.0, *) {
@@ -490,8 +503,8 @@ struct IntendedPremiumWidget: Widget {
                 PremiumWidgetEntryView(entry: entry)
             }
         }
-        .configurationDisplayName("Intended — Detailed")
-        .description("See your habits, progress, and daily message.")
+        .configurationDisplayName(LocalizedStringKey("widget.detailed.name"))
+        .description(descriptionKey)
         .supportedFamilies([.systemMedium, .systemLarge])
     }
 }
